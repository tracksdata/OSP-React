let BUTTONS=[10,20,30,40,50,-200,-12];
/*fetch('http://localhost:8080/api/menus')
    .then(response => response.json)
    .then(buttons => {
      buttons.map((btn) => {
        BUTTONS.push(btn);
      });
        console.log(BUTTONS);
        console.log('====>');
        

    })
    */
export default BUTTONS;